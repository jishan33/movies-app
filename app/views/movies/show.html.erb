<h1> Show page </h1>


<h1><%= @movie.name %></h1>
<h1><%= @movie.country.name  %>

 <div class="show-edit-delete">
   <% if can? :edit, @movie %>
     <%= link_to "Edit", edit_movie_path(@movie.id) %>
     <%= link_to "Delete", movie_path(@movie.id), method: :delete %>
   <% end %>
   <%= link_to "Back", movies_path %>
 </div>

